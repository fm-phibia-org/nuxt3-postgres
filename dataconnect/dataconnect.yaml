specVersion: "v1beta"
serviceId: "nuxt-data-connect"
location: "asia-northeast1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "test-db"
      cloudSql:
        instanceId: "test-sql-instance"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
